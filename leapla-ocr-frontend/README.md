# OpenCodeReasoning2 Frontend

Simple React application to act as the UI gateway to interact with the finetuned model.

## Setup

Install dependencies:

```bash
npm install
```

Run the development server:

```bash
npm start
```

The app will run locally at http://localhost:3000

## Note
The UI is not connected to AI at the moment.